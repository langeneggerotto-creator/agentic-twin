"""Dream Builder: a local-first LLM assistant that turns a goal into a
plan, finds the resources needed to pursue it, tracks progress, and
reflects — using an LLM running on your own machine (via Ollama), reaching
out to the internet only for the specific lookups that need it.
"""
