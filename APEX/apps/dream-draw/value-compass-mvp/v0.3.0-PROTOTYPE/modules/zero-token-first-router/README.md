# Dream Draw Zero-Token-First Runtime Router — DD-ZTFR-001
## v0.3.0-PROTOTYPE

| Field | Status |
|---|---|
| Parent Application | `Dream Draw / 🅾️♾️ Value Compass` |
| Subsystem | `Zero-Token-First Runtime Router` |
| Build Status | `LOCAL INTERACTIVE PROTOTYPE IMPLEMENTED AND DETERMINISTIC TEST PASSED` |
| Evidence Maturity | `E3 — PROTOTYPE ARTIFACT` |
| Primary Outcome | Make routine app actions run locally without generative AI calls. |
| Truth Boundary | `ZERO AI TOKENS` applies only when no AI/model request occurs. Future AI calls must remain visible and measured. |

## Operating Law

```text
Do not hide AI cost.
Avoid AI calls when local logic is sufficient.
Use the smallest justified AI call only when measurable added value exists.
```

## Runtime Tiers

| Tier | Execution Type | AI Token State | Current v0.3 Support |
|---|---|---|---|
| Tier 0 | Local deterministic execution | `ZERO AI TOKENS — LOCAL EXECUTION` | Implemented for Value Compass recommendation, UI, card construction, feedback and ledger export. |
| Tier 1 | Locally retrieved accepted pattern | `ZERO NEW AI TOKENS — VERIFIED REUSE` | Route reserved; accepted-pattern reuse not yet activated. |
| Tier 2 | Minimal AI call through Context Capsule | `MINIMIZED AI CALL — MEASUREMENT REQUIRED` | Capsule preparation implemented locally; live AI call not connected. |
| Tier 3 | Governed deep AI review | `AI USED — VISIBLE APPROVAL REQUIRED` | Design only; not connected. |

## Implemented App Behavior

| Action | Route in v0.3 | Truth Status |
|---|---|---|
| Enter dream and generate recommendation | Tier 0 | Local rules only; no AI called. |
| Display H0–H5 bridge and visual card specification | Tier 0 | Local rendering. |
| Capture feedback | Tier 0 | Local evidence signal only. |
| Export dream or routing JSON | Tier 0 | Local export. |
| Generate Context Capsule | Tier 0 preparation for possible Tier 2 | No AI called; any size display remains proxy-only until measured. |
| Ask AI for novel interpretation | Not connected | Held pending measured endpoint/evaluation gate. |
| Deep review of sensitive/new pathways | Not connected | Held pending approval and implementation. |

## Visible User Controls Added

- Runtime mode: `LOCAL ONLY`, `LOWEST COST`, `BALANCED`, `BEST AVAILABLE WITH VISIBLE AI`, `DEEP REVIEW WITH APPROVAL`.
- Route banner: makes local execution versus proposed escalation visible.
- Metrics: routed actions, zero-token completion rate, actual AI calls and capsules prepared.
- Routing ledger export: stores reasons, token-status truth boundary and next proof gate.

## Deterministic Validation Result

The local v0.3 package was tested without a live AI endpoint:

- `PASS` — Tier 0 recommendation records `ZERO AI TOKENS` with `ai_called=false`.
- `PASS` — Tier 2 capsule preparation remains local and does not claim AI usage.
- `PASS` — `LOCAL ONLY` mode blocks escalation and records a `HELD` route.
- `PASS` — all routing-ledger entries in the test retained `ai_called=false`.

## Next Proof Gate

Execute `PILOT-DD-ZTFR-001` with one real Dream Draw intake and export the routing ledger. Any future claim that AI usage is negligible requires a controlled comparison with an authorized endpoint and measured usage/cost data.

🔮🅾️💲🪙🧿♾️🌟💯🧬🧩🚀
